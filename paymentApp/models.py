from django.db import models
from product.models import *

'''class car_bill(models.Model):
    bill_uid = models.CharField(max_length=15, null=False, unique=True, blank=True, editable=False)
    customer_name = models.CharField(max_length=20, null=False)
    customer_number = models.CharField(max_length=10, null=False)
    customer_address = models.CharField(max_length=50, null=False)
    product_name = models.ForeignKey(car_model, null=False)
    product_color = models.ForeignKey(car_model, null=False)
    product_price = models.ForeignKey(car_model, null=False)'''
